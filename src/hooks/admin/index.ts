/**
 * Admin Hooks - Barrel Export
 *
 * CMS mutation hooks have been removed (documentation, landing, navigation, footer, status).
 * Only admin auth and dashboard hooks remain.
 */

// Admin hooks are imported directly from their files:
// - useAdminAuth from '../useAdminAuth'
// No admin mutation hooks remain after CMS removal.
